/// <reference types="react-scripts" />

type Todo = {
  userId: number,
  id: number,
  title: string,
  completed: boolean,
};

type User = {
  id: number,
  name: string,
  phone: string,
  email: string,
};
