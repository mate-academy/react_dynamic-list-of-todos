/// <reference types="react-scripts" />

interface Todo {
  id: number,
  userId: number,
  completed: boolean,
  title: string,
}

interface User {
  id: number,
  name: string,
  email: string,
  phone: string,
}
