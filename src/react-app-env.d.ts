/// <reference types="react-scripts" />

type Todo = {
  id: number,
  userId: number,
  title: string,
  completed: bollean,
};

interface User {
  id: number,
  name: string,
  username: string,
  username: string | null,
  email: string,
  phone: string,
}
