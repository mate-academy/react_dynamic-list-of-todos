/// <reference types="react-scripts" />

type Todo = {
  id: string,
  userId: string,
  title: string,
  completed: boolean,
};

type User = {
  id: number,
  name: string,
  email: string,
  phone: string,
};
