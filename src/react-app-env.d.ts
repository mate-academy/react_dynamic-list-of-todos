/// <reference types="react-scripts" />

type Todo = {
  userId: number,
  id: number,
  title: string,
  completed: boolean
};

type User = {
  id: number,
  email: string,
  name: string,
  phone: string,
  username: string,
};
