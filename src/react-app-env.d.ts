export type Todo = {
  userId: number,
  id: number,
  title: string,
  completed: boolean,
};

export type User = {
  id: number,
  name: string,
  username: string,
  email: string,
  phone: string,
  website: string,
};
