/// <reference types="react-scripts" />

interface Todo {
  userId: number;
  id: number;
  title: string;
  completed: boolean;
}

interface User {
  name: string;
  email: string;
  phone: string;
  id: number;
}
