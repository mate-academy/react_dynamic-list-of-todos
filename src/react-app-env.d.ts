/// <reference types="react-scripts" />

type Todo = {
  title: string,
  id: number,
  userId: number,
  completed: boolean,
};

interface User {
  name: string
  username: string,
  email:string,
  phone:string,
}
