/// <reference types="react-scripts" />

type Todo = {
  id: number,
  completed: boolean,
  userId: number,
  title: string,
};

type User = {
  id: number,
  name: string,
  email: string,
  phone: string,
};
