/// <reference types="react-scripts" />

type Todo = {
  id: number,
  userId: number,
  completed: boolean,
  title: string,
};

type User = {
  id: number;
  name: string;
  phone: string;
  email: string;
};
