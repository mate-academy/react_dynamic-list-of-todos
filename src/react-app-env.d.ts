/// <reference types="react-scripts" />

type Todo = {
  title: string,
  completed: boolean,
  userId: number,
  id: number,
};

interface User {
  name: string,
  phone: number,
  email: string,
  id: number,
};
