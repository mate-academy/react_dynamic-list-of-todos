/// <reference types="react-scripts" />

type Todo = {
  completed: boolean,
  id: number,
  title: string,
  userId: number,
};

type User = {
  name: string,
  email: string,
  phone: string,
  id: number,
};
