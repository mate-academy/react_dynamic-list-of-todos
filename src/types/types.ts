export type Todo = {
  id: number,
  completed: boolean,
  title: string,
  userId: number,
};

export type User = {
  id: number,
  name: string,
  username: string,
  email: string,
  phone: string,
};
